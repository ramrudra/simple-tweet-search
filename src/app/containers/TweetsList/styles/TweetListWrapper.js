import styled from 'styled-components';

// Color Consts
import { colorConsts } from 'appUtils/appConsts';

const TweetListWrapper = styled.div`
  margin-top: 20px;
  display: flex;
  flex-flow: row wrap;
  justify-content: space-between;
`;

export default TweetListWrapper;
